<h2>Submit a Review</h2>

<h3>Write a review for your favorite review </h3>

<div>
  <%= form_for [@restaurant, @review] do |f| %>
    <p>
      <%= f.label :rating, "Rating:" %>
      <%= f.text_field :rating %>
    </p>
    <p>
      <%= f.label :body, "Review:" %>
      <%= f.text_area :body, size: "60x12" %>
    </p>

  <%= f.submit "Create" %>
<% end %>
</div>
